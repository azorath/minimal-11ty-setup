{
    "name": "minimal-11ty-setup",
    "version": "0.1.0",
    "private": true,
    "description": "Minimal Eleventy Setup with SCSS support",
    "engines": {
        "node": ">=16.0.0",
        "npm": ">=7.6.0"
    },
    "scripts": {
        "build": "eleventy",
        "serve": "eleventy ./src --serve --port=8000"
    },
    "devDependencies": {
        "@11ty/eleventy": "0.12.1",
        "fs-extra": "10.0.0",
        "node-sass": "6.0.1"
    }
}
